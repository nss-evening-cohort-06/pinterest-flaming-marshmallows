'use strict';

app.controller("BoardCtrl", function ($rootScope, $scope, PinterestService) {

  // const getBoards = () => {
  //   PinterestService.getBoards($rootScope.uid).then((results) => {
  //     $scope.boards = results;
  //   }).catch((error) => {
  //     console.log("Error in PinterestService", error);
  //   });
  // };

  // getBoards();

});